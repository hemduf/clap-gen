name: Rustfmt check (manual)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rustfmt:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Rust formatting
        run: cargo fmt --all --check
